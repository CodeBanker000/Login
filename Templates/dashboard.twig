{% extends "layout/layout.twig" %}

{% block content %}
<header class="p-3 gradient-custom-2 text-white">
    <div class="container">
      <div class="col col-12 d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <h1 class="col col-10">Benvenuto {{ name }}</h1>

        <div class="col text-end">
            <form action="/logout" method="post" >
                <button class="btn btn-outline-light me-2" type="submit">Logout</button>
            </form>
        </div>
      </div>
    </div>
  </header>
{% endblock %}